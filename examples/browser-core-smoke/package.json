{
  "name": "volcano-browser-core-smoke",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "npm run check && vite build",
    "dev": "vite --host 127.0.0.1",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "volcano-volcano": "file:../../build/js/packages/volcano-volcano"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vite": "^8.0.0"
  }
}
